{
	"name": "fmulcd-web-client",
	"version": "1.0.0",
	"description": "Start the player. To start development web server on port 3000 run the command in folder 'configdev': npm start",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "npx mix",
		"watch": "npx mix watch",
		"prod": "npx mix --production"
	},
	"author": "Jonathan Roth",
	"license": "ISC",
	"devDependencies": {
		"laravel-mix": "^6.0.49",
		"sass": "^1.67.0",
		"sass-loader": "^12.6.0"
	},
	"dependencies": {
		"axios": "^1.5.0",
		"bootstrap": "^5.3.2"
	}
}
